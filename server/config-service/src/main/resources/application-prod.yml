spring:
  config:
    activate:
      on-profile: prod
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Friendly-neighborhood-development/games_store_production_environment_configurations
          skipSslValidation: true

  security:
    user:
      password: admin
