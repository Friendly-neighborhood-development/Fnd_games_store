spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      server:
        git:
          uri: file://home/sergey/work/workspace-idea/games_store_environment_configurations
          skipSslValidation: true
