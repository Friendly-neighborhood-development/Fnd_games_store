spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
server:
  servlet:
    context-path: /cart
  port: 8083

variables:
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrapAddress: "localhost:9092"