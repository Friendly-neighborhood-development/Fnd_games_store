spring:
  kafka:
    bootstrap-servers: kafka:9092
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
server:
  servlet:
    context-path: /cart
  port: 8083

variables:
  redis:
    host: localhost
    port: 6379
