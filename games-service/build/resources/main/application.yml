spring:
  profiles:
    active: dev

  flyway:
    user: postgres
    password: root
    schemas: app-db
    url: jdbc:postgresql://localhost:5432/games_service_database
    locations: filesystem:db/migration
    enabled: true

