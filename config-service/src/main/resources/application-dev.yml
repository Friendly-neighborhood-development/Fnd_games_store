spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Friendly-neighborhood-development/games_store_development_environment_configurations
          skipSslValidation: true

  security:
    user:
      password: admin
